---

 - name: Pre-check Connectivity
   hosts: linux-servers
   become: true
   gather_facts: false

   tasks:
    - name: 

 - name: Update RedHat Like Linux Servers - Starting Deploy
   hosts: redhat
   become: true
   gather_facts: false

   tasks: 
    - name: DNF | Update System
      ansible.builtin.dnf:
        name: "*"
        state: latest
        update_cache: true
      tags: update    
          
 - name: Update Debian Like Linux Servers - Starting Deploy
   hosts: debian
   become: true
   gather_facts: false

   tasks: 
    - name: APT | Update System
      ansible.builtin.apt: 
       name: "*"
       state: latest
       update_cache: true
      tags: update
